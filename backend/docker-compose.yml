services:
  postgres:
    image: postgres:16
    container_name: recash-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: recash
    ports:
      - "5433:5432"
    volumes:
      - recash_pgdata:/var/lib/postgresql/data
volumes:
  recash_pgdata:
